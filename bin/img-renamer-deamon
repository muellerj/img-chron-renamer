#!/bin/zsh

# Watch all files in given file hirarchy
fswatch --exclude=.DS_Store -0 $1 | \
while read -d "" event
do
  eventdir=`dirname ${event}`
  echo "Change in ${eventdir}"
  #img-chron-rename ${eventdir}
done
