#!/bin/zsh

fswatch --exclude=.DS_Store -0 . | \
while read -d "" event
do
  eventdir=`dirname #{event}`
  echo "Change in ${eventdir}"
  ls ${eventdir}/*.JPG | while read file; do mv ${file} ${file:l}; done
  img-chron-rename `dirname ${event}`/*.jpg
done
